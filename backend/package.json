{
  "name": "sos-backend",
  "version": "1.0.0",
  "description": "Express backend bridging the Expo SOS app to Bolna's outbound-call API. Triggers calls, receives Bolna webhook on call end, dispatches RSA + ambulance based on agent's decision.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
